class Lesson {
  String title;
  String level;
  double indicatorValue;
  int price;
  String content;
  String image;
  Lesson(
      {this.title, this.level, this.indicatorValue, this.price, this.content, this.image});
}